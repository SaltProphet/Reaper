# REAPER Environment Variables Example
# 
# Copy this file to `.env` and fill in your actual values.
# NEVER commit .env file to version control!
#
# This file documents all environment variables used by REAPER core and
# reference plugins. Your custom plugins may require additional variables.

# ==============================================================================
# CORE CONFIGURATION
# ==============================================================================

# Python environment
# Uncomment to set Python path if needed
# PYTHONPATH=/path/to/reaper

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# REAPER_LOG_LEVEL=INFO

# Enable debug mode (more verbose logging)
# Default: false
# REAPER_DEBUG=false

# ==============================================================================
# PLUGIN DISCOVERY
# ==============================================================================

# Plugin directories (colon-separated paths)
# Default: ./plugins:~/.reaper/plugins
# REAPER_PLUGIN_PATH=./plugins:./custom-plugins

# Auto-load plugins from plugin path
# Default: true
# REAPER_AUTO_LOAD_PLUGINS=true

# ==============================================================================
# SIGHT PLUGINS (Visual Detection)
# ==============================================================================

# Example: Reddit Ingestor Plugin
# Detects signals from Reddit subreddits
# REDDIT_INGESTOR_CLIENT_ID=your-reddit-client-id
# REDDIT_INGESTOR_CLIENT_SECRET=your-reddit-client-secret
# REDDIT_INGESTOR_USER_AGENT=your-app-name/1.0
# REDDIT_INGESTOR_ENDPOINT=https://oauth.reddit.com
# REDDIT_INGESTOR_TIMEOUT=30

# Example: Web Scraper Plugin
# Scrapes web pages for signals
# WEB_SCRAPER_USER_AGENT=ReaperBot/1.0
# WEB_SCRAPER_TIMEOUT=30
# WEB_SCRAPER_MAX_RETRIES=3

# Example: GitHub Issues Plugin
# Monitors GitHub issues and PRs
# GITHUB_ISSUES_TOKEN=your-github-token
# GITHUB_ISSUES_API_URL=https://api.github.com
# GITHUB_ISSUES_TIMEOUT=30

# ==============================================================================
# HEARING PLUGINS (Audio/Text Detection)
# ==============================================================================

# Example: Discord Monitor Plugin
# Monitors Discord channels for text signals
# DISCORD_MONITOR_BOT_TOKEN=your-discord-bot-token
# DISCORD_MONITOR_GUILD_ID=your-guild-id
# DISCORD_MONITOR_TIMEOUT=30

# Example: Slack Monitor Plugin
# Monitors Slack channels for text signals
# SLACK_MONITOR_BOT_TOKEN=your-slack-bot-token
# SLACK_MONITOR_WORKSPACE_ID=your-workspace-id

# Example: Twitter Stream Plugin
# Monitors Twitter streams for keywords
# TWITTER_STREAM_API_KEY=your-twitter-api-key
# TWITTER_STREAM_API_SECRET=your-twitter-api-secret
# TWITTER_STREAM_ACCESS_TOKEN=your-access-token
# TWITTER_STREAM_ACCESS_SECRET=your-access-secret

# ==============================================================================
# TOUCH PLUGINS (Interaction Detection)
# ==============================================================================

# Example: User Activity Tracker
# Tracks user interactions and engagement
# ACTIVITY_TRACKER_DB_URL=postgresql://user:pass@localhost/db
# ACTIVITY_TRACKER_SESSION_TIMEOUT=1800

# Example: API Usage Monitor
# Monitors API endpoint usage patterns
# API_MONITOR_METRICS_ENDPOINT=http://localhost:9090/metrics

# ==============================================================================
# TASTE PLUGINS (Quality/Sampling Detection)
# ==============================================================================

# Example: Code Quality Analyzer
# Analyzes code quality metrics
# CODE_QUALITY_SONAR_URL=http://localhost:9000
# CODE_QUALITY_SONAR_TOKEN=your-sonar-token

# Example: Performance Monitor
# Monitors application performance metrics
# PERF_MONITOR_APM_URL=http://localhost:8200
# PERF_MONITOR_SERVICE_NAME=reaper-service

# ==============================================================================
# SMELL PLUGINS (Pattern/Anomaly Detection)
# ==============================================================================

# Example: Anomaly Detector
# Detects anomalies in time-series data
# ANOMALY_DETECTOR_MODEL_PATH=/path/to/model
# ANOMALY_DETECTOR_THRESHOLD=0.95

# Example: Trend Analyzer
# Analyzes trends in signal patterns
# TREND_ANALYZER_WINDOW_SIZE=100
# TREND_ANALYZER_MIN_SAMPLES=10

# ==============================================================================
# SCORING PLUGINS
# ==============================================================================

# Example: ML Scorer Plugin
# Scores signals using machine learning model
# ML_SCORER_MODEL_PATH=/path/to/model.pkl
# ML_SCORER_THRESHOLD=0.7

# Example: Rule-Based Scorer
# Scores signals using predefined rules
# RULE_SCORER_CONFIG_PATH=/path/to/rules.yaml

# Example: Priority Scorer
# Scores based on priority keywords
# PRIORITY_SCORER_HIGH_KEYWORDS=urgent,critical,emergency
# PRIORITY_SCORER_LOW_KEYWORDS=minor,trivial,cosmetic

# ==============================================================================
# ACTION PLUGINS
# ==============================================================================

# Example: Discord Notifier Plugin
# Sends notifications to Discord webhook
# DISCORD_NOTIFIER_WEBHOOK_URL=https://discord.com/api/webhooks/...
# DISCORD_NOTIFIER_TIMEOUT=10
# DISCORD_NOTIFIER_RETRY_ATTEMPTS=3

# Example: Slack Notifier Plugin
# Sends notifications to Slack webhook
# SLACK_NOTIFIER_WEBHOOK_URL=https://hooks.slack.com/services/...
# SLACK_NOTIFIER_TIMEOUT=10

# Example: Email Notifier Plugin
# Sends email notifications
# EMAIL_NOTIFIER_SMTP_HOST=smtp.gmail.com
# EMAIL_NOTIFIER_SMTP_PORT=587
# EMAIL_NOTIFIER_USERNAME=your-email@example.com
# EMAIL_NOTIFIER_PASSWORD=your-app-password
# EMAIL_NOTIFIER_FROM_ADDRESS=noreply@example.com
# EMAIL_NOTIFIER_USE_TLS=true

# Example: GitHub Issues Creator
# Creates GitHub issues for high-priority signals
# GITHUB_CREATOR_TOKEN=your-github-token
# GITHUB_CREATOR_REPO=owner/repo
# GITHUB_CREATOR_LABELS=signal,automated

# Example: PagerDuty Integration
# Creates incidents in PagerDuty
# PAGERDUTY_INTEGRATION_KEY=your-integration-key
# PAGERDUTY_API_URL=https://api.pagerduty.com

# Example: Webhook Dispatcher
# Sends signals to custom webhooks
# WEBHOOK_DISPATCHER_URL=https://your-service.com/webhook
# WEBHOOK_DISPATCHER_SECRET=your-webhook-secret
# WEBHOOK_DISPATCHER_TIMEOUT=15

# ==============================================================================
# STORAGE & CACHING
# ==============================================================================

# Redis for caching (optional)
# REDIS_URL=redis://localhost:6379/0
# REDIS_TIMEOUT=5
# REDIS_MAX_CONNECTIONS=10

# PostgreSQL for signal storage (optional)
# DATABASE_URL=postgresql://user:pass@localhost:5432/reaper
# DATABASE_POOL_SIZE=5
# DATABASE_MAX_OVERFLOW=10

# File-based signal storage (optional)
# SIGNAL_STORAGE_PATH=/var/lib/reaper/signals
# SIGNAL_STORAGE_MAX_SIZE_MB=1000

# ==============================================================================
# RATE LIMITING
# ==============================================================================

# Global rate limits (requests per minute)
# API_RATE_LIMIT_RPM=100
# WEBHOOK_RATE_LIMIT_RPM=60

# ==============================================================================
# MONITORING & OBSERVABILITY
# ==============================================================================

# Metrics export (Prometheus format)
# METRICS_ENABLED=true
# METRICS_PORT=9090
# METRICS_PATH=/metrics

# Distributed tracing (OpenTelemetry)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_SERVICE_NAME=reaper
# OTEL_TRACES_SAMPLER=always_on

# Logging configuration
# LOG_FORMAT=json  # or "text"
# LOG_OUTPUT=/var/log/reaper/reaper.log  # or "stdout"

# ==============================================================================
# SECURITY
# ==============================================================================

# API authentication (if exposing HTTP API)
# API_AUTH_ENABLED=true
# API_AUTH_TOKEN=your-secret-api-token

# Webhook signature verification
# WEBHOOK_SIGNING_SECRET=your-webhook-secret

# TLS/SSL configuration
# TLS_CERT_PATH=/path/to/cert.pem
# TLS_KEY_PATH=/path/to/key.pem

# ==============================================================================
# DEVELOPMENT & TESTING
# ==============================================================================

# Enable test mode (uses mock data instead of real APIs)
# REAPER_TEST_MODE=false

# Enable profiling
# REAPER_PROFILE=false

# Enable hot reload (watches for code changes)
# REAPER_HOT_RELOAD=false

# ==============================================================================
# NOTES
# ==============================================================================
#
# 1. SECURITY: Never commit actual secrets to version control!
#    - Use this file as a template only
#    - Copy to `.env` and add `.env` to `.gitignore`
#    - Use secret management tools in production (Vault, AWS Secrets Manager, etc.)
#
# 2. PLUGIN-SPECIFIC: Each plugin may require additional variables
#    - Check plugin README for complete list
#    - Plugin variables follow pattern: [PLUGIN_NAME]_[VARIABLE_NAME]
#
# 3. DEFAULTS: Most variables have sensible defaults
#    - Only set variables you need to customize
#    - Refer to plugin documentation for default values
#
# 4. FORMAT: 
#    - No quotes needed: VAR=value (not VAR="value")
#    - No spaces around =: VAR=value (not VAR = value)
#    - Comments use # at start of line
#
# 5. LOADING:
#    - Use python-dotenv: `pip install python-dotenv`
#    - Load in code: `from dotenv import load_dotenv; load_dotenv()`
#    - Or use system environment variables directly
#
# 6. VALIDATION:
#    - REAPER validates required variables at startup
#    - Pydantic models validate format and constraints
#    - Missing required variables cause clear error messages
#
# ==============================================================================

# Your custom variables below:
